<!DOCTYPE html>
<html lang="pt-BR" class="transition-colors duration-300">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      darkMode: 'class'
    }
  </script>
  <title>Princesa Reborn - Erro</title>
</head>
<body class="bg-gray-100 dark:bg-gray-900 text-gray-900 dark:text-gray-100 flex flex-col min-h-screen font-sans transition-colors duration-300">

  <%- include('partials/header') %>

  <main class="flex-grow flex items-center justify-center px-4 py-16">
    <div class="bg-white p-8 rounded-lg shadow-md max-w-md text-center">
      <h1 class="text-4xl mb-4 font-bold text-red-600 flex items-center justify-center gap-3">
        <span><%= icone %></span>
        <span><%= titulo %></span>
      </h1>
      <p class="mb-6 text-gray-700 text-lg"><%= mensagem %></p>

      <div class="mb-6">
        <img
          src="/imagens/<%= imagem %>"
          alt="<%= titulo %>"
          class="mx-auto max-h-48 object-contain"
        />
      </div>

      <div>
        <a
          href="/dashboard"
          class="inline-block bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-md shadow transition"
        >
          Voltar para a página inicial
        </a>
      </div>
    </div>
  </main>
  <script src="/js/darkmode.js"></script>
  <%- include('partials/whatsapp') %>

  <%- include('partials/footer') %>
</body>
</html>
